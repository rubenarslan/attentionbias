<IfModule mod_rewrite.c>
   RewriteEngine on
# uncomment the below three lines on the server
#   RewriteCond %{HTTPS} off
#   RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI} [R,L]
#   RewriteBase /
   RewriteRule    ^$ app/webroot/    [L]
   RewriteRule    (.*) app/webroot/$1 [L]
</IfModule>